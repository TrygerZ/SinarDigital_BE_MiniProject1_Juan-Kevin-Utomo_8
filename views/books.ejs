<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Buku</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: #f0f0f0; }
        header { background: #333; color: white; padding: 20px; text-align: center; }
        nav { background: #555; padding: 10px; text-align: center; }
        nav a { color: white; text-decoration: none; margin: 0 15px; }
        nav a:hover { text-decoration: underline; }
        .container { max-width: 900px; margin: 20px auto; background: white; padding: 20px; border-radius: 5px; }
        h1 { color: #333; margin-bottom: 20px; }
        .stats { display: flex; gap: 20px; margin-bottom: 20px; }
        .stat-box { background: #f5f5f5; padding: 15px; border-radius: 3px; flex: 1; text-align: center; }
        .stat-box h3 { color: #333; }
        .stat-box p { color: #666; font-size: 0.9em; }
        .book-item { background: #f9f9f9; padding: 15px; margin-bottom: 10px; border-left: 4px solid #333; }
        .book-item h3 { color: #333; margin-bottom: 5px; }
        .book-item p { color: #666; margin: 3px 0; }
        .empty { color: #999; text-align: center; padding: 20px; }
        footer { background: #333; color: white; text-align: center; padding: 10px; margin-top: 20px; }
    </style>
</head>
<body>
    <header>
        <h1>📚 Perpustakaan Digital</h1>
    </header>
    
    <nav>
        <a href="/">Home</a>
        <a href="/books">Daftar Buku</a>
        <a href="/contact">Kontak</a>
    </nav>
    
    <div class="container">
        <h1>Daftar Buku</h1>
        
        <% if (stats) { %>
            <div class="stats">
                <div class="stat-box">
                    <h3><%= stats.total %></h3>
                    <p>Total Buku</p>
                </div>
                <div class="stat-box">
                    <h3><%= stats.avgYear %></h3>
                    <p>Rata-rata Tahun</p>
                </div>
                <div class="stat-box">
                    <h3><%= stats.newest %></h3>
                    <p>Buku Terbaru</p>
                </div>
            </div>
        <% } %>
        
        <% if (books && books.length > 0) { %>
            <% books.forEach(book => { %>
                <div class="book-item">
                    <h3><%= book.title %></h3>
                    <p><strong>Penulis:</strong> <%= book.author %></p>
                    <p><strong>Tahun:</strong> <%= book.year %></p>
                </div>
            <% }); %>
        <% } else { %>
            <div class="empty">
                <p>Belum ada buku. Tambahkan buku baru melalui API!</p>
            </div>
        <% } %>
    </div>
    
    <footer>
        <p>&copy; 2025 Perpustakaan Digital</p>
    </footer>
</body>
</html>
